// Коментарий.

/*
    Огромный
    Коментарий
    !!!!!!!!!!
*/

/* 
Переменые.
    	int переменая используемая для вывода целого числа или для математических функций до 2 милиардов.
    	uint переменая только для положительных целых чисел.
    	byte переменая только для положительных целых чисел но с огроничением до 255 включительно нужно для оптимизации кода.
    	short переменая только для положительных целых чисел но с огроничением до 3600 включительно нужно для оптимизации кода.
	long переменая до нескольких трилионов такая переменая не используеться.
	float переменая для чисел с точкой запись 4.5345f; f обозночает что переменая именно float тоесть с точкой числа могут быть как положительными так и отрицательными огроничения слишком велеки.
	double переменая для чисел с точкой запись 500000.53000045d; d обозночает что переменая именно double тоесть с точкой числа могут быть как положительными так и отрицательными огроничения в несколько раз больше чем float.
	string переменая для строк или предложений также можно оставить один символ надо записывать только "Тут что то написано";
	shar переменая для одного символа здесь уже используються 'H';.
	bool переменая куда записываеться только trut;(Правда) или false;(Ложь) используеться восновном в условиях или же в циклах.
	
	
	
*/

// Чтобы заработала программа нужно сохронить файл сочетанием клавиш ctrl + S.

// Примичание сочетание клавиш надо вводить в окне кода а не в терминале.

// Console.WriteLine будет выводить в разных строках.

// Console.Write будет выводить в одной строке.

// (\n) переводит на новую строку используеться в Console.Write("Тут что-то написано\n");. 

/*
    Включаем автосохранение.
    В боковой панели выберите шестерёнку, перейдите в «Параметры» → «Текстовый редактор» → «Файлы».
    Вы увидите вкладку Auto Save, в которой нужно указать период сохранения файлов.
    Мы рекомендуем режим afterDelay с задержкой в одну секунду — так вы будете сразу сохранять всё, что запишете.
*/

// Премечание переменая int не будет работать без имени пример (int number = -10;) должно выглядить примерно как-то так.
 
/* Менять значение переменой можно сколько угодно раз например int number = -10;
                                                                number = 10;
                                                                number = 5;
                                                                И тд.
    Если мы хотим чтобы выводилось каждое значение переменой то тогда это должно выглядеть так: int number = -10;
                                                                                                Console.WriteLine(number);
                                                                                                number = 10;
                                                                                                Console.WriteLine(number);
                                                                                                number = 5;
                                                                                                Console.WriteLine(number);
                                                                                                И тд.
*/

// Примечание это можно делать с любыми типами данных хоть с целами числами как сделано выше так и со сторками и тд.

//  Чтобы вывести текст вместе с переменной нужно дописать (+) Console.WriteLine("Переменная: " + number);












using System;

namespace project {

    class Program {

        static void Main() {
            int num_1 = 0, num_2 = 0;

            num_1 = Convert.ToInt32(Console.ReadLine()); // Конвертирует в 32 битную числовую переменую int ибо ReadLine = выводит в кансоль только строки.
            num_2 = Convert.ToInt32(Console.ReadLine()); 
            Console.WriteLine("First: " + num_1 + ". Second: " + num_2);
            
        }

    }

}
